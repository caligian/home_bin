#bin/bash

loc="BCM43142A0-0a5c-216d.hcd"
dest="/lib/firmware/bcrm"

cp $loc $dest
echo "Successfully copied the driver."
echo -ne "Restart (no for no)?\n>> "
read yes
[[ $yes =~ no ]] && exit 0 || reboot
